# Docker Compose Fix Task&#10;&#10;## Steps:&#10;- [x] Step 1: ✅ docker-compose.yml fixed (duplicate container_name removed, structure cleaned, dev services under profiles).&#10;- [ ] Step 2: Run `sudo docker compose down`.&#10;- [ ] Step 3: Run `sudo docker compose up -d`.&#10;- [ ] Step 4: Verify `sudo docker compose ps`.&#10;&#10;Progress: docker-compose.yml is now valid YAML. Test from /monitoring dir.
